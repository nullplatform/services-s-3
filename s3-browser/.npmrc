# @nullplatform/plugin is on public npmjs; a global ~/.npmrc that routes the scope to
# GitHub Packages (the org's private registry) would make resolution fail.
@nullplatform:registry=https://registry.npmjs.org
